services:
  - type: web
    plan: free
    name: nexus-portfolio
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: NODE_VERSION
        value: 20
